name: First workflow

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: echo "Hello, world!"

  other:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: ls -alt