<%= turbo_stream.update 'comments', '' if @product.comments.one? %>
<%= turbo_stream.prepend 'comments', partial: 'comments/comment', locals: { comment: @comment } %>
<%= render_turbo_stream_flash_messages %>
